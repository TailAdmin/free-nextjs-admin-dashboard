describe('Dashboard Tests', () => {
  test('renders correctly', () => {
    expect(true).toBe(true);
  });

  test('app name is defined', () => {
    const appName = 'free-nextjs-admin-dashboard';
    expect(appName).toBeDefined();
  });
});